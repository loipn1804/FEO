<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/AppScreenStyle"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >

        <include layout="@layout/view_header" />

        <ListView
            android:id="@+id/lvShopbyCountry"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/short_distance_margin_padding"
            android:divider="@null"
            android:dividerHeight="@dimen/short_distance_margin_padding"
            android:listSelector="@android:color/transparent" />
    </LinearLayout>

    <include layout="@layout/view_progressbar" />

</RelativeLayout>