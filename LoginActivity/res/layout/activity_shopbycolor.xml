<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/AppScreenStyle"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include layout="@layout/view_header" />

        <TextView
            android:id="@+id/color_select"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/long_short_distance_margin_padding"
            android:layout_marginTop="@dimen/long_short_distance_margin_padding"
            android:gravity="center"
            android:text="Select a color"
            android:textSize="@dimen/textsize_larger"
            android:typeface="serif" />

        <ListView
            android:id="@+id/lvColor"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none"></ListView>
    </LinearLayout>

    <include layout="@layout/view_progressbar" />

</RelativeLayout>